{
  "name": "rag-techniques-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "basic-rag:ingest": "pnpm --filter basic-rag run ingest",
    "basic-rag:query": "pnpm --filter basic-rag run query",
    "csv-rag:ingest": "pnpm --filter csv-rag run ingest",
    "csv-rag:query": "pnpm --filter csv-rag run query",
    "reliable-rag:ingest": "pnpm --filter reliable-rag run ingest",
    "reliable-rag:query": "pnpm --filter reliable-rag run query",
    "chunk-optimizer:benchmark": "pnpm --filter chunk-optimizer run benchmark",
    "proposition-chunking:ingest": "pnpm --filter proposition-chunking run ingest",
    "proposition-chunking:query": "pnpm --filter proposition-chunking run query"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "ts-node": "^10.9.2",
    "turbo": "^2.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.0.14"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "openai": "^4.67.0"
  }
}
