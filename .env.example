# Global environment variables for the RAG Techniques monorepo

# OpenAI API key used by shared LLM utilities
OPENAI_API_KEY=sk-your-openai-key

# Optional log level for shared logger: debug | info | warn | error
LOG_LEVEL=info

# Optional override for RAG config path (per CLI can set its own)
#RAG_CONFIG_PATH=projects/basic-rag/config/basic-rag.config.json